
# Base URL of the backend API. Leave blank for local dev (Vite proxies /api and
# /socket.io to http://localhost:4000 — see vite.config.ts). In production this
# must be the full origin of the deployed backend, e.g. https://api-errances.socialmm.in
VITE_API_URL=
